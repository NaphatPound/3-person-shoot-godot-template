# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Large binary assets via Git LFS (Mixamo FBX + extracted textures)
*.fbx filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
